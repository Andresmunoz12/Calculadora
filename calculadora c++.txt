codigo de c++ para calculadora
#include <iostream>
using namespace std;

int main() {
    double a, b;
    int opcion;
    bool salir = false;

    cout << "Calculadora simple (solo iostream)\n";

    while (!salir) {
        cout << "\nElige una opcion:\n";
        cout << "1) Sumar\n";
        cout << "2) Restar\n";
        cout << "3) Multiplicar\n";
        cout << "4) Dividir\n";
        cout << "5) Salir\n";
        cout << "Opcion: ";
        cin >> opcion;
if (opcion == 5) {
            salir = true;
            cout << "Saliendo...\n";
            continue;
        }

        cout << "Ingresa el primer numero: ";
        cin >> a;
        cout << "Ingresa el segundo numero: ";
        cin >> b;

        switch (opcion) {
            case 1:
                cout << "Resultado: " << a + b << "\n";
                break;
            case 2:
                cout << "Resultado: " << a - b << "\n";
                break;
            case 3: